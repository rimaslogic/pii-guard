name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"

      - name: Install test deps
        run: pip install 'pytest>=7,<10'

      - name: Run tests (gate on green)
        run: pytest -q

      - name: Verify VERSION matches tag
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          TAG_VERSION="${TAG#v}"
          FILE_VERSION="$(cat VERSION | tr -d '[:space:]')"
          if [ "$TAG_VERSION" != "$FILE_VERSION" ]; then
            echo "Tag $TAG_VERSION does not match VERSION file $FILE_VERSION" >&2
            exit 1
          fi

      - name: Build release artifacts
        run: bash scripts/build-release.sh

      - name: Extract changelog section
        id: changelog
        run: |
          VERSION="$(cat VERSION | tr -d '[:space:]')"
          # pull the section between [X.Y.Z] and the next [ heading
          awk -v v="$VERSION" '
            $0 ~ "^## \\[" v "\\]" { grab=1; next }
            grab && /^## \[/ { exit }
            grab { print }
          ' CHANGELOG.md > RELEASE_NOTES.md
          echo "---" >> RELEASE_NOTES.md
          echo "**SHA-256 checksums:**" >> RELEASE_NOTES.md
          echo '```' >> RELEASE_NOTES.md
          cat "dist/pii-guard-${VERSION}.sha256" >> RELEASE_NOTES.md
          echo '```' >> RELEASE_NOTES.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
        with:
          body_path: RELEASE_NOTES.md
          files: |
            dist/pii-guard-*.zip
            dist/pii-guard-*.sha256
          draft: false
          prerelease: false
